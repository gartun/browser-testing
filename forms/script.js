const btn = document.querySelector("button"),
      required = document.querySelectorAll("[required]");

// iterate over all required fields
// and start the validating process
required.forEach(field => {
  
});